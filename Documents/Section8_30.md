# Notes about MCU bus interfaces

STM32 F446RE processor is ARM Cortex M4
Has 3 types of Bus: I Bus (instruction bus), D bus (Data bus), and S bus (System Bus)

- Constants are stored in ROM (flash memory).
- Non-constant data is stored in SRAM.
